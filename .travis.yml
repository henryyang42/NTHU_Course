language: python
#services:
    #- mysql
python:
    - "2.7"
    - "pypy"
install:
    - pip install --upgrade pip --use-mirrors
    - pip install -r requirement.txt --use-mirrors
    #- pip install mysql-python
    - pip install pylint --use-mirrors
    - pip install pylint-django --use-mirrors
    - pip install pyflakes --use-mirrors
before_script:
    - pyflakes .
script:
    - sh ./ci-test/pylint.sh
    #- sh ./ci-test/runTest.sh
notifications:
    email:
        recipients:
            - z54981220@gmail.com
            - henryyang42@gmail.com
        on_success: [always]
        on_failure: [always]

